--- a/dlls/ntdll/unix/process.c
+++ b/dlls/ntdll/unix/process.c
@@ -23,6 +23,11 @@
 #include "ntdll_unix.h"
 #include "wine/debug.h"
+#ifdef __aarch64__
+/* Reduce unnecessary context switches within Wine's internal components */
+#define CONTEXT_SWITCH_REDUCTION 1
+#define MAX_IDLE_PROCESS_CONTEXT_SWITCHES 10 /* Aggressively suspend idle processes */
+#endif
+
 WINE_DEFAULT_DEBUG_CHANNEL(ntdll);
