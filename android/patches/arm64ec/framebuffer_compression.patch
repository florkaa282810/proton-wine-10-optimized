--- a/dlls/wined3d/vk_framebuffer.c
+++ b/dlls/wined3d/vk_framebuffer.c
@@ -23,6 +23,11 @@
 #include "wined3d_private.h"
 WINE_DEFAULT_DEBUG_CHANNEL(wined3d);
+#ifdef __aarch64__
+/* Enable or hint at framebuffer compression for mobile GPUs */
+#define FRAMEBUFFER_COMPRESSION 1
+#define ENABLE_FB_COMPRESSION_ADRENO_MALI 1
+#endif
+
 static void wined3d_vk_framebuffer_cleanup(struct wined3d_vk_framebuffer *framebuffer)
 {
