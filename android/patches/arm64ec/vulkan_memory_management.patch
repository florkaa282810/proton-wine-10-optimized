--- a/dlls/wined3d/vk_memory.c
+++ b/dlls/wined3d/vk_memory.c
@@ -23,6 +23,11 @@
 #include "wined3d_private.h"
 WINE_DEFAULT_DEBUG_CHANNEL(wined3d);
+#ifdef __aarch64__
+/* Optimize Vulkan memory allocation for mobile GPUs (e.g., prefer DEVICE_LOCAL) */
+#define VK_MEMORY_OPTIMIZATION 1
+#define PREFER_DEVICE_LOCAL_MEMORY 1
+#endif
+
 static void wined3d_vk_memory_cleanup(struct wined3d_vk_memory *memory)
 {
