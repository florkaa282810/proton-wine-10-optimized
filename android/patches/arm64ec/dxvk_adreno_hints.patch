--- a/dlls/wined3d/vk_device.c
+++ b/dlls/wined3d/vk_device.c
@@ -23,6 +23,11 @@
 #include "wined3d_private.h"
 WINE_DEFAULT_DEBUG_CHANNEL(wined3d);
+#ifdef __aarch64__
+/* Add DXVK/VKD3D hints for Adreno/Mali specific optimizations */
+#define ADRENO_MALI_DXVK_HINTS 1
+#define VK_IMAGE_LAYOUT_OPTIMAL_FOR_TILE_RENDER 1 /* Custom layout hint */
+#endif
+
 static void wined3d_vk_device_cleanup(struct wined3d_vk_device *device)
 {
