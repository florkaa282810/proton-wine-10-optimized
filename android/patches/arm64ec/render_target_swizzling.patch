--- a/dlls/wined3d/vk_image.c
+++ b/dlls/wined3d/vk_image.c
@@ -23,6 +23,11 @@
 #include "wined3d_private.h"
 WINE_DEFAULT_DEBUG_CHANNEL(wined3d);
+#ifdef __aarch64__
+/* Optimize render target swizzling for Adreno/Mali architectures */
+#define RENDER_TARGET_SWIZZLING_OPTIMIZATION 1
+#define ENABLE_OPTIMIZED_SWIZZLE_PATH 1
+#endif
+
 static void wined3d_vk_image_cleanup(struct wined3d_vk_image *image)
 {
