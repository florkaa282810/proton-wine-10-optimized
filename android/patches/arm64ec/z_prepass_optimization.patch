--- a/dlls/wined3d/vk_state.c
+++ b/dlls/wined3d/vk_state.c
@@ -23,6 +23,11 @@
 #include "wined3d_private.h"
 WINE_DEFAULT_DEBUG_CHANNEL(wined3d);
+#ifdef __aarch64__
+/* Optimize Z-prepass rendering to reduce overdraw, especially at high resolutions */
+#define Z_PREPASS_OPTIMIZATION 1
+#define ENABLE_EARLY_Z_TEST 1
+#endif
+
 static void wined3d_vk_state_cleanup(struct wined3d_vk_state *state)
 {
